-- Creamos una nueva base de datos:
-- CREATE DATABASE app; 

-- Creamos la tabla para guardar a los usuarios:
-- CREATE TABLE app.usuarios(
--     id_usuario INT AUTO_INCREMENT PRIMARY KEY,
--     nombre_usuario VARCHAR(150), 
--     email VARCHAR(75), 
--     password_usuario VARCHAR(150), 
--     create_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
-- );

-- -- Le indicamos la base de datos que debe usar: 
-- USE app;

-- -- Le metemos valores:
-- INSERT INTO app.usuarios (nombre_usuario, email, password_usuario) VALUES 
--     ('Juanma', 'kartofell@gmail.com', 'AAAAAAAAAA'),
--     ('Aida', 'Perro_marron@outlook.es', 'BBBBBBBBBB');